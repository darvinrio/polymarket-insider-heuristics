This is a Python Project.

1. Only use `uv`. Running any python script should use `uv run`.

repo commands examples:

```sh
uv run plot/plot_sus_score_percentiles.py
```

2. Always only use repo for storing anything. Do not use other folders like `/tmp`.
